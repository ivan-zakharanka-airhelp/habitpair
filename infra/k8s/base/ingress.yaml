apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: rate-limit
  namespace: habitpair
spec:
  rateLimit:
    average: 100
    burst: 50
---
apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: rate-limit-auth
  namespace: habitpair
spec:
  rateLimit:
    average: 10
    period: 1m
    burst: 5
---
apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: security-headers
  namespace: habitpair
spec:
  headers:
    frameDeny: true
    contentTypeNosniff: true
    browserXssFilter: true
    stsSeconds: 31536000
    stsIncludeSubdomains: true
---
apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: api-strip
  namespace: habitpair
spec:
  stripPrefix:
    prefixes:
      - /api
---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: api-gateway
  namespace: habitpair
spec:
  entryPoints:
    - websecure
  routes:
    # Every path is service-scoped: /api/<service>/... Unknown paths 404 at the
    # gateway instead of silently falling through to one of the services.
    # Auth routes — stricter rate limit (brute-force defense on login).
    - match: Host(`api.habitpair.com`) && PathPrefix(`/api/auth`)
      kind: Rule
      middlewares:
        - name: rate-limit-auth
        - name: security-headers
        - name: api-strip
      services:
        - name: auth-api-service
          port: 3000
    # Habits routes
    - match: Host(`api.habitpair.com`) && PathPrefix(`/api/habits`)
      kind: Rule
      middlewares:
        - name: rate-limit
        - name: security-headers
        - name: api-strip
      services:
        - name: habits-api-service
          port: 3000
  tls:
    certResolver: letsencrypt

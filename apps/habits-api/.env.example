# Database
DATABASE_URL=postgresql://dev:dev@localhost:5434/habits

# App
PORT=3001
NODE_ENV=development

# Shared with auth-api: HS256 secret used to verify the Bearer JWT.
# In K8s this comes from secret `auth-jwt-secret` (key: secret).
JWT_SECRET=dev-jwt-secret-change-me-in-production
